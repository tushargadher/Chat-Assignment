import { Box, Typography, styled, TextField, Button } from "@mui/material";
export const OptionWrap = styled(Box)(({ theme }) => ({
  // backgroundColor: "gray",
  display: "flex",
  marginTop: "5px",
}));
